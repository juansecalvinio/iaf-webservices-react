{
  "name": "iaf-webservices",
  "version": "1.0.0",
  "author": "jcalvino",
  "license": "ISC",
  "scripts": {
    "build": "NODE_ENV=production yarn dev:server",
    "client": "cd client && yarn start",
    "server": "cd server && npx nodemon app.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "dev:server": "cd client && yarn build && cd .. && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
